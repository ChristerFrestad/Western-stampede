# Deprecated location — Portainer looks for compose at repo root by default.
# Use: ../docker-compose.yml
# Kept so older docs still resolve if compose path is set to deploy/docker-compose.yml

include:
  - path: ../docker-compose.yml
